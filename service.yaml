apiVersion: v1
kind: Service
metadata:
  name: udagram-frontend
  labels:
    app: udagram-frontend
spec:
  selector:
    app: udagram-frontend
  ports:
    - port: 8080
      protocol: TCP
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
